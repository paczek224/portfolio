[project]
name = "portfolio-agents"
version = "0.1.0"
description = "Minimal multi-agent orchestrator for generating a portfolio site from a CV."
requires-python = ">=3.11"
dependencies = [
    "pypdf>=5.0.0",
]

[tool.pytest.ini_options]
pythonpath = ["src"]
